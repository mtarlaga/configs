require("mtarlaga.plugins-setup")
require("mtarlaga.core.options")
require("mtarlaga.core.keymaps")
require("mtarlaga.core.colorscheme")

-- sourcing from vim (in lua file)
-- vim.cmd('source ~/.config/nvim/config.vim')

-- sourcing from lua (in vim file)
-- lua require('init') --this will load the file .config/nvim/lua/init.lua.
